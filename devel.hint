category: Libs
requires: cygwin libjthread1.2.1
external-source: jthread
sdesc: "C++ cross-platform thread library (development)"
ldesc: "The JThread package provides some classes to make use of threads 
easy on different platforms. The classes are actually rather simple 
wrappers around existing thread implementations."
